# It is collection of well define object or non-repetitive elements

s={2,3,4}
# set and dic are define by {} then to create emty set 
# s = {} will create emty dictionary
e = set()
s1={2,2,3,5,7,7,7} # output it will not repeat the same value it will print only one time
print(s1)

'''1. Sets are unordered => Element’s order doesn’t matter
2. Sets are unindexed => Cannot access elements by index
3. There is no way to change items in sets.
4. Sets cannot contain duplicate values.'''
